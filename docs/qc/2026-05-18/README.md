# QC Index — 2026-05-18

- `ares-chief-of-staff-v0/` — read-only Ares Chief of Staff lead desk digest, artifact writer, and dedicated Slack route. Verification: focused/regression tests `51 passed`, full backend `1143 passed`, configured artifact-root dry-run side-effect check `dry_run_artifacts_created=0`, `git diff --check`/`git diff --cached --check` passed. No live provider sends, paid skiptrace, Slack posts, Supabase remote migration, VPS deploy, or Telegram delivery performed.
