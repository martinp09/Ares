# QC Index — 2026-05-18

- `ares-chief-of-staff-v0/` — read-only Ares Chief of Staff lead desk digest, artifact writer, and dedicated Slack route. Verification: focused/regression tests `51 passed`, full backend `1143 passed`, configured artifact-root dry-run side-effect check `dry_run_artifacts_created=0`, `git diff --check`/`git diff --cached --check` passed. No live provider sends, paid skiptrace, Slack posts, Supabase remote migration, VPS deploy, or Telegram delivery performed.
- `ares-chief-of-staff-v1-employee-reporting/` — turns the digest into a Slack-first employee report with worklog, priorities, blockers, approval requests, read-only lead-machine health context, Slack PII redaction, and no read-only missing-state lock writes. Verification: focused/regression tests `52 passed`, full backend `1144 passed`, configured artifact-root/source-run-state dry-run side-effect check `dry_run_artifacts_created=0` and `dry_run_source_state_created=0`, `git diff --check` passed. No live provider sends, paid skiptrace, live source calls, Slack posts, Supabase remote migration, VPS deploy, or Telegram delivery performed.
